apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: payments-api
  namespace: apps
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: payments-api
    namespace: flux-system
  valuesFrom:
    - kind: ConfigMap
      name: payments-api-values
