apiVersion: score.dev/v1b1
metadata:
  name: score-checkout-stage
containers:
  web:
    image: example.local/score-checkout:stage
service:
  ports:
    web:
      port: 8080
resources:
  postgres:
    type: postgres
