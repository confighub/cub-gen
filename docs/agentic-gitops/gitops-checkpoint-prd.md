# GitOps Checkpoint PRD (Legacy Naming Bridge)

**Status:** Legacy bridge note  
**Date:** 2026-03-30  
**Audience:** Product, platform engineering, runtime engineering

`gitops-checkpoint-prd` was an earlier name for the governed mutation and
receipt model that now shows up across `cub-track`, the checkpoint schema page,
and the broader Agentic GitOps design docs.

Use this file as a compatibility bridge when older notes still refer to the
checkpoint PRD name.

## What to read now

1. `docs/agentic-gitops/02-design/00-agentic-gitops-design.md`
2. `docs/agentic-gitops/05-rollout/10-cub-track.md`
3. `docs/agentic-gitops/04-schemas/00-gitops-checkpoint-schemas.md`
4. `docs/agentic-gitops/cub-track-mvp-upsell-and-dual-store.md`

## Naming note

The newer docs prefer language such as:

1. mutation ledger
2. `ChangeInteractionCard`
3. governed change transaction
4. receipts and attestations

That is the current terminology. `gitops-checkpoint-prd` remains here so older
references are not stale.
